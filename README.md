# SoccerLens
## 무료 배포 방법
1. GitHub에서 새 저장소를 만든다.
2. 이 폴더의 app.py와 requirements.txt를 업로드한다.
3. Streamlit Community Cloud에서 GitHub 계정으로 로그인한다.
4. Create app에서 저장소를 선택하고 Main file을 `app.py`로 지정한다.
5. Deploy를 누른다.

배포 후 생성된 주소는 PC와 휴대폰에서 바로 열 수 있습니다.

CSV 형식:
`passer,recipient,count`
